using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Data;
using System.Data.SQLite;


namespace PILFINAL
{
    public     class Autentificar
    {
       /* public static int autenticar(string pUsuarios, string pContraseña)
        {
            int resultado = -1;
            var sql = "select * from registro_alumnos WHERE correo='{0}' and PwdCompare('{1}',password=1", pUsuarios; pContraseña)con);
            SQLiteCommand cmd = new SQLiteCommand(sql, conexion.InstanciaDb());

            return 0;

    */

        }
    }

